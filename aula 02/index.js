// strings
const email = "gabriel@gmail.com";
console.log(email);

// concatenação de stings
const firtsName = "gabriel";
const seccondName = "mori";
const fullName = firtsName + " " + seccondName;
// usar sinal de + para concatenar
console.log(fullName);

// acessando caracteres
console.log(fullName[0]);

// comprimento de uma String
console.log(fullName.length);

// metodos de string
// toUppercase();
// transforma todas as caracteres em maiusculo, não altera o valor original

// toLowerCase();
// transforma todas as caracteres em minusculo, não altera o valor original

// indexOf();
const index = email.indexOf("@");
console.log(index);
// pegar a posição do index do caracter
